<style>
    
    .formHome {
        text-align: center;
    }

    .centrado {
        text-align: center;
    }

    .select-container {
        justify-content: center;
        /* Centra los elementos horizontalmente */
        display: flex;
        /* Utilizamos flexbox para colocar los select en línea */
        gap: 10px;
        /* Espacio entre los select */
    }

    .select-box {
        height: 30px;
        /* Altura personalizada */
        font-size: 16px;
        /* Tamaño de fuente personalizado */
        border-radius: 5px;
        /* Bordes redondeados */
        text-align: center;
        border: none;
        outline: none;
        width: 200px;
        /* Ancho de cada select */
        padding: 5px;
        /* Espacio interno */
        font-size: 14px;
        /* Tamaño de fuente */
    }

    body {
        /*background-image: url('https://images.unsplash.com/photo-1548588627-f978862b85e1?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1471&q=80'); /* Reemplaza 'ruta_de_la_imagen.jpg' con la ruta de tu imagen */
        background-size: cover;
        /* Ajusta la imagen para cubrir todo el fondo */
        background-attachment: fixed;
        /* Fija la imagen de fondo para que no se desplace con el contenido */
        background-position: center;
        /* Centra la imagen de fondo */
        /*color: #fff; /* Cambia el color del texto para que sea legible en la imagen de fondo */
        padding: 20px;
        /* Agrega un relleno al encabezado para separarlo del contenido */
        text-align: justify;
        /* Centra el contenido del encabezado */

        overflow-x: hidden;

    }

    /*-----------------------------------------------*/

    .boton {
        padding: 1.3em 3em;
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        font-weight: 500;
        color: #000;
        background-color: #fff;
        border: none;
        border-radius: 10px;
        box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease 0s;
        cursor: pointer;
        outline: none;
    }

    .boton:hover {
        background-color: #152e9e;
        box-shadow: 0px 15px 20px rgba(6, 16, 153, 0.4);
        color: #fff;
        transform: translateY(-7px);
    }

    .boton:active {
        transform: translateY(-1px);
    }

    /*--------------------*/
    /*barra de navigation*/
    nav {
        background-color: #f8f4f4;
        border-radius: 6px;
        /* Cambia el color a tu elección */
        padding: 10px;
        /* Agrega un relleno (padding) para separar el contenido del fondo */
    }
    /*--------------------*/
    /*icono*/
</style>